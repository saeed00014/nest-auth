export enum PasswordRuleNames {
  maxLength = 'maxLength',
  minLength = 'minLength',
  minNumberCount = 'minNumberCount',
  minSpecialCharCount = 'minSpecialCharCount',
  minUpperCaseLetterCount = 'minUpperCaseLetterCount',
}
